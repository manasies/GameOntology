<div class="container">
    <h1>UserList</h1>
    <p>Select an User</p>

    <ul class="list-group">
        {{#each users}}
        <li class="list-group-item"><a href="/user/{{id}}">{{real_name}} ({{username}})</a></li>
        {{/each}}
    </ul>
</div>